[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "signalforge"
version = "0.1.0"
description = "Structured web-change intelligence for HTML and JSON endpoints."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Vaelrixx"}]
dependencies = []

[project.scripts]
signalforge = "signalforge.cli:main"
